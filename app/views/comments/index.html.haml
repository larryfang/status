%h1 Listing comments

%table
  %tr
    %th Content
    %th
    %th
    %th

  - @comments.each do |comment|
    %tr
      %td= comment.content
      %td= link_to 'Show', comment
      %td= link_to 'Edit', edit_comment_path(comment)
      %td= link_to 'Destroy', comment, :class =>'delete_comment',:confirm => 'Are you sure?', :method => :delete, :remote => true

%br

= link_to 'New Comment', new_comment_path
